# Fill with real values; keep everything in one region close to Romania.
PROJECT_ID=your-project-id
REGION=europe-central2          # Warsaw (closest). If unavailable, use europe-west3 (Frankfurt).
DATAFLOW_BUCKET=your-dataflow-bucket
BIGTABLE_INSTANCE=your-bigtable-instance
BIGTABLE_TABLE=product_stats
COMPOSER_ENV=your-composer-env
TEMPLATE_PATH=gs://your-dataflow-bucket/templates/streaming_pipeline_flex_template.json
